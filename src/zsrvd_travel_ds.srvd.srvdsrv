@EndUserText.label: 'SERVICE DEFINATION FOR PROJECTION TRAVEL'
define service ZSRVD_TRAVEL_DS {
  expose ZPCDS_TRAVEL_DS;
  expose ZPCDS_BOOKINGS_DS as Booking;
  expose I_Currency        as Currency;
  expose I_Country         as Country;
}